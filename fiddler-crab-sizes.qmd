---
title: "lterdatasampler"
format: html
editor: visual
execute: 
  echo: false
---

## 

```{r}
library(lterdatasampler)
library(tidyverse)
```

```{r}
View(pie_crab)
```

```{r}
#| fig-cap: "this is my caption"

ggplot(pie_crab, aes(x = latitude, y = size)) +
  geom_point()

  
```
```{r}
library(kableExtra)

pie_crab %>% group_by(site) %>% 
  summarise(mean = round(mean(size), 2),
            standard_dev = sd(size),
            sample_size = n()) %>% 
  kable(col.names = c("Size", "Mean", "Standard Deviation", "Sample Size")) 
            
```

